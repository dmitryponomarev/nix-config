{ lib, config, ... } : {
  plugins = {

    indent-blankline = {
      enable = true;
    };

    web-devicons = {
      enable = true;
    };

    oil = {
      enable = true;
    };

    neoscroll = {
      enable = true;
    };

    treesitter = {
      enable = true;
    };

    nvim-autopairs = {
      enable = true;
    };
  };
}
